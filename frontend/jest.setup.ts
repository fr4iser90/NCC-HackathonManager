import '@testing-library/jest-dom';

// Optional: If you need to mock global fetch, you can do it here or use a library like jest-fetch-mock
// For example:
// global.fetch = jest.fn(() =>
//   Promise.resolve({
//     json: () => Promise.resolve({ some: 'data' }),
//   })
// );

// You can also add any other global setup or mocks here 